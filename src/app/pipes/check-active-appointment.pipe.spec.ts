import { CheckActiveAppointmentPipe } from './check-active-appointment.pipe';

describe('CheckActiveAppointmentPipe', () => {
  it('create an instance', () => {
    const pipe = new CheckActiveAppointmentPipe();
    expect(pipe).toBeTruthy();
  });
});
