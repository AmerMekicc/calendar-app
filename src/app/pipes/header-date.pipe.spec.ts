import { HeaderDatePipe } from './header-date.pipe';

describe('HeaderDatePipe', () => {
  it('create an instance', () => {
    const pipe = new HeaderDatePipe();
    expect(pipe).toBeTruthy();
  });
});
