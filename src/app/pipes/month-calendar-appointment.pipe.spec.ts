import { MonthCalendarAppointmentPipe } from './month-calendar-appointment.pipe';

describe('MonthCalendarAppointmentPipe', () => {
  it('create an instance', () => {
    const pipe = new MonthCalendarAppointmentPipe();
    expect(pipe).toBeTruthy();
  });
});
