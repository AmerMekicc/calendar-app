import { CheckActiveWeekDayPipe } from './check-active-week-day.pipe';

describe('CheckActiveWeekDayPipe', () => {
  it('create an instance', () => {
    const pipe = new CheckActiveWeekDayPipe();
    expect(pipe).toBeTruthy();
  });
});
