import { CheckActiveWeekPipe } from './check-active-week.pipe';

describe('CheckActiveWeekPipe', () => {
  it('create an instance', () => {
    const pipe = new CheckActiveWeekPipe();
    expect(pipe).toBeTruthy();
  });
});
