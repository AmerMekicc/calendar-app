import { CheckActiveDayMonthPipe } from './check-active-day-month.pipe';

describe('CheckActiveDayMonthPipe', () => {
  it('create an instance', () => {
    const pipe = new CheckActiveDayMonthPipe();
    expect(pipe).toBeTruthy();
  });
});
